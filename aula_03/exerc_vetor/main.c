#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <locale.h>

// Definir constante, só pra valores inteiros
#define TAM 10

/*int main()
{
    setlocale(LC_ALL, "Portuguese");

    // Declarar vetor com 10 posições
    int numeros[TAM], i, maior = 0;

    // Inserir os 10 valores
    for(i = 0; i < TAM; i++){
        printf("Informe o valor: ");
        scanf("%d", & numeros[i]);
    }

    // Encontrar o maior valor
    for(i = 0; i < TAM; i++){
        if(numeros[i] > maior){
            maior = numeros[i];
        }
    }

    printf("O maior valor é: %d", maior);

    return 0;
}
*/
